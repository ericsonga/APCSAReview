.. qnum::
   :prefix: 2-8-
   :start: 1
   
Summary
-----------------------

In this chapter you started to learn a bit about Java - the programming language you need to learn for the AP CS A exam.  

.. dragndrop:: ch2_vocab
    :feedback: This is feedback.
    :match_1: object|||An object does the actual work in an object-oriented program.  The class creates objects.  An object is an instance of a class.
    :match_2: class|||A class defines a type and is used to define what all objects of that class know and can do.
    :match_3: Java|||A programming language you can use to tell a computer what to do.  It is the language used on the AP CS A exam.
    :match_4: compiler|||Translates a Java source file (ends in .java) from something a human can read into a Java class file (ends in .class) which is a format a computer (Java run time) can read.
    
    Drag the name to the correct defintion.  